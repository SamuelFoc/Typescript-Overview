console.log("Hello, TS config!");

let i: number = 0;

// In tsconfig.json set up the rootDir to be ./src and outDir to be ./dist
// (you can set the path as you want) so it will automatically look in root dir
// when compiling the code and save it to the outDir
