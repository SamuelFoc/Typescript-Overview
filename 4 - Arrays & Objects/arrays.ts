// Define an array of strings
let names: string[] = ["a", "b", "c"];

// Define an array of numbers
let ages: number[] = [1, 2, 3];

// TS also supports infering of types (automatically set to string[])
let fruits = ["apple", "banana", "mango"];
